@model IEnumerable<Project.Models.MmTmcdDescripciones>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StrCultura)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StrDescripcionMaterial)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StrUsuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DtFecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IntIdTipoMaterialCaracteristica)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StrCultura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StrDescripcionMaterial)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StrUsuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DtFecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IntIdTipoMaterialCaracteristica)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IntIdMmTmcdDescripciones">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IntIdMmTmcdDescripciones">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IntIdMmTmcdDescripciones">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
