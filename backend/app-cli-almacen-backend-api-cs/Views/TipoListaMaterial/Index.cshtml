@model IEnumerable<Project.Models.TipoListaMaterial>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StrNombreTipoLista)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StrDescripcionLista)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StrNombreTipoLista)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StrDescripcionLista)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IntIdTipoListaMaterial">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IntIdTipoListaMaterial">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IntIdTipoListaMaterial">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
